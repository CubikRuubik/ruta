{
  "name": "ruta-workspace",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd apps/desktop && npm install",
    "dev": "concurrently \"npm run dev:tauri\" \"npm run dev:backend\"",
    "dev:backend": "cd apps/backend/indexer && cargo run",
    "dev:tauri": "cd apps/desktop && npm install && npm run tauri dev",
    "build": "npm run build:backend && npm run build:tauri && npm run postbuild",
    "build:backend": "cd apps/backend/indexer && cargo build --release",
    "build:tauri": "cd apps/desktop && npm install && npm run tauri build",
    "postbuild": "node ./postbuild.mjs",
    "lint": "cd apps/frontend && npm run lint",
    "test": "cd apps/backend && npm run test"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
